import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'war_20211064',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
